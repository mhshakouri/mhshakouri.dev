# Contact form (M6). Create a key at https://resend.com — free tier is plenty.
# Without a key the form returns a friendly "not available" message.
RESEND_API_KEY=

# Optional overrides. Defaults: from onboarding@resend.dev (works without
# domain verification), to the public site email.
CONTACT_FROM_EMAIL=
CONTACT_TO_EMAIL=
